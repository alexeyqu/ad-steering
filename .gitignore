node_modules/
.next/
dist-scanner/

# Auth state — never commit session tokens
.auth/

# Local data — goals, scans, plans, logs (never commit)
data/
public/screenshots/

# Env
.env
.env.local
.env*.local

# OS
.DS_Store
Thumbs.db
